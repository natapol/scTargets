% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{tar_sc_single_qc}
\alias{tar_sc_single_qc}
\title{Perform Single-Cell Quality Control for 10x Genomics Data}
\usage{
tar_sc_single_qc(
  x10_file_path,
  empty_droplets_fdr_threshold = 0.01,
  empty_lower_droplets = TRUE,
  replace_unfiltered = TRUE,
  save_dataset_sensitive_filtering = TRUE,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{x10_file_path}{Character. Path to the 10x Genomics data directory.}

\item{empty_droplets_fdr_threshold}{Numeric. False discovery rate (FDR) threshold for identifying
empty droplets. Default is 0.01.}

\item{empty_lower_droplets}{Logical. Whether to use a lower threshold for detecting empty droplets.
Default is TRUE.}

\item{replace_unfiltered}{Logical. Whether to replace existing unfiltered data columns in the
SingleCellExperiment object. Default is TRUE.}

\item{save_dataset_sensitive_filtering}{Logical. Whether to save the dataset-sensitive filtered
dataset instead of the custom filtered dataset. Default is TRUE.}

\item{BPPARAM}{A BiocParallel parameter object specifying parallelization settings. Default is
\code{BiocParallel::SerialParam()}.}
}
\value{
A list of \code{targets} objects representing the QC pipeline steps, including:
\itemize{
\item \code{path}: The input file path.
\item \code{sce_raw}: The raw SingleCellExperiment object.
\item \code{empty_droplets}: Results of empty droplet detection.
\item \code{sce_no_empty_drop}: SingleCellExperiment object after removing empty droplets.
\item \code{per_cell_qc_metrics}: Per-cell QC metrics.
\item \code{sce_unfiltered}: SingleCellExperiment object with unfiltered data and QC metrics.
\item \code{sce_sensitive_filter}: Dataset-sensitive filtered SingleCellExperiment object.
\item \code{sce_custom_filter}: Custom filtered SingleCellExperiment object.
\item \code{sce_filtered}: Final filtered SingleCellExperiment object based on the selected filtering strategy.
}
}
\description{
This function performs a series of quality control (QC) steps on single-cell RNA sequencing data
generated by 10x Genomics. It includes reading the data, detecting and removing empty droplets,
calculating per-cell QC metrics, applying dataset-sensitive and custom filters, and generating
filtered datasets.
}
\details{
The function performs the following steps:
\enumerate{
\item Reads the 10x Genomics data and creates a raw SingleCellExperiment object.
\item Detects empty droplets using the \code{DropletUtils::emptyDrops} function.
\item Removes empty droplets based on the specified FDR threshold.
\item Calculates per-cell QC metrics, including mitochondrial and ribosomal gene percentages.
\item Applies dataset-sensitive and custom filters to identify low-quality cells.
\item Generates filtered datasets based on the selected filtering strategy.
}
}
\examples{
\dontrun{
tar_sc_single_qc(
  x10_file_path = "/path/to/10x/data",
  empty_droplets_fdr_threshold = 0.01,
  empty_lower_droplets = TRUE,
  replace_unfiltered = TRUE,
  save_dataset_sensitive_filtering = TRUE,
  BPPARAM = BiocParallel::SerialParam()
)
}

}
